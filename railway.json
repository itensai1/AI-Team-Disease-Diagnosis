{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pipenv run uvicorn disease_dignoses.api:app  && pipenv run streamlit run app_ui.py"
  }
}
